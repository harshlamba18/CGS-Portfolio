# CGS Portfolio — Vite + React + React Three Fiber

Quick start:

```bash
npm install
npm run dev
```

Open http://localhost:5173 and you should see a rotating box rendered with `@react-three/fiber`.
